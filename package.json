{
  "name": "raycarwash-project",
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix frontend && npm install --prefix backend",
    "install-deps": "python -m venv backend/venv && ./backend/venv/Scripts/pip install -r backend/requirements.txt",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && cross-env ENV_FILE=backend/.env ./venv/Scripts/python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev:frontend": "npm start --prefix frontend",
    "dev:marketing": "npm run dev --prefix marketing",
    "dev:web": "npm run dev --prefix web",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:web\" \"npm run dev:marketing\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3"
  }
}
