{
  "name": "raycarwash-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-deps": "python -m venv venv && venv\\Scripts\\pip install -r requirements.txt",
    "dev": "venv\\Scripts\\python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  }
}
